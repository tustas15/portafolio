specVersion: "v1"
serviceId: "portafolio-tustas"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "portafolio-tustas-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
